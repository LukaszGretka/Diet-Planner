export interface Measurement {
    weight: number;
    chest: number;
    belly: number;
    waist: number;
    biceps: [number, number];
    forearm : [number, number];
    thigh: [number, number];
    calf: [number, number];
}